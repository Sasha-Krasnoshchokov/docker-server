## The application uses the pnpm as a package manager

## Dev local server

- dev local server runs in the nodemon mode to monitor changes on the fly
- the dev server deployment process is described in the docker-compose.dev.yml file
- to launch the dev server use the command
```bash
docker compose -f docker-compose.dev.yml up --build
```
